# Детерминированная установка: agent-сессии и CI не должны молча менять граф зависимостей.
engine-strict=true
save-exact=true
resolution-mode=highest
# Lifecycle scripts зависимостей — исполняемый код (ADR-008). Разрешаем только явный allowlist.
side-effects-cache=false
