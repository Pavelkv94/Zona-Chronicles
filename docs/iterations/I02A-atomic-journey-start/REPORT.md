# I02A — REPORT

Итерация: I02A «Атомарный старт journey»
Ветка: `iteration/I00-harness` (`PLAN.md` §8, отклонение 1)
Вход: I01 (`cfe76fc`)
Дата: 2026-08-22
Статус: **верификация пройдена, находки закрыты; рекомендован короткий повторный проход**

## 1. Что впервые стало возможным наблюдать

Мир перестал жить только внутри процесса. Команда меняет durable state атомарно, и результат
виден другому процессу:

```text
$ pnpm world init --seed 42
Мир world:prototype создан из seed=42; версия 0.

$ pnpm world run --agent agent:rook --route route:yard-to-bridge
Команда cmd_8B8S7CT1T6920H6HJVCM7MCN8S принята.
  события: evt_D2ETY739KPRAYTMXP916Q7DKTN
  версия мира: 0 -> 1

$ pnpm world state          # отдельный процесс, отдельное подключение
  agent:rook     traveling  в loc:quiet-yard по маршруту route:yard-to-bridge

$ pnpm world run --agent agent:rook --route route:yard-to-bridge --command-id cmd_8B8S...
Команда cmd_8B8S7CT1T6920H6HJVCM7MCN8S принята (повтор: результат прочитан из journal).
  события: evt_D2ETY739KPRAYTMXP916Q7DKTN

$ pnpm world events
Событий: 1
```

Мир по-прежнему не идёт сам: `journey.completed` требует scheduler-а — это I02B. Доказано, что
первый настоящий переход состояния переживает процесс, повтор и сбой.

## 2. Какой тест был красным до реализации

```text
Error: Cannot find module './migrate.ts' imported from
  packages/persistence/src/migrate.integration.test.ts
```

Дальше Red приходил не со старта, а от проб, которые проверяли сами проверки:

| Что | Red | Green |
| --- | --- | --- |
| Append-only грантами | `grant … update, delete on world_events` — тест B7 проходит | отказ `permission denied` |
| Прохождение через БД (B8) | `routeId: null` в репозитории — checksum расходится | тест ловит расхождение |
| Драйвер в манифесте ядра | `pg` в `@zona/domain/package.json` — `boundaries:check` exit 0 | нарушение названо |
| Формат id | сравнение двух инстансов одной реализации проходит при любой смене алгоритма | golden-значения |
| Идемпотентность | чужое тело под записанным `command_id` возвращало «принято» и чужие `event_ids` | `precondition_failed` без единой записи |
| Уровень изоляции | под REPEATABLE READ/SERIALIZABLE — брошенный 40001 вместо названного отказа | явный `read committed`, тест на трёх уровнях |
| Роли БД | приложение ходило под SUPERUSER с BYPASSRLS | весь путь под `zona_worker`, `update`/`delete` отвергаются правами |
| Сборка перед acceptance | без `dist` порождённый CLI не стартует, а `build` стоял в CI последним | названная причина + build до тест-шагов |
| Secret scan | файл с настоящим паролем и credential-URL проходил | оба паттерна ловят, заглушки — нет |

## 3. Автоматические gates

| Gate | Результат |
| --- | --- |
| `pnpm verify` | 0 — 635 unit, 29 property, 438 contract, security, build |
| `pnpm test:integration` | 0 — 46 тестов на настоящем PostgreSQL 17.5 |
| `pnpm test:acceptance` | 0 — 27/27 |
| `pnpm boundaries:check` | 0 |

## 4. Что увидел человек при demo

Заполняет владелец. Скрипт — `PLAN.md` §2.

## 5. Отклонения и риски

Отклонения 1–3 записаны в `PLAN.md` §8 ДО начала реализации, 4–6 — по ходу, в момент, когда
были допущены или обнаружены. Самое существенное из поздних: **применённая миграция 0003
переписана**, что фреймворк прямо запрещает. Сделано осознанно и ровно один раз, пока поставок
не существует, чтобы не зафиксировать пароль ролей в истории репозитория навсегда. После первой
поставки такое отклонение недопустимо.

Ошибка lead-а, найденная им самим: первая версия миграций дублировала уже существующий
migration runner из I00 и перезаписала `packages/persistence/src/database.ts` — вывод о
содержимом пакета был сделан по усечённому `find … | head -40`. Исправлено до первого коммита.

Отдельный риск, не устранённый этой итерацией: **верификация шла в общем рабочем дереве**.
SubagentStop дважды приписал architecture-reviewer-у правки lead-а, и вторая блокировка едва не
потеряла его отчёт. Смягчено (эскалация ограничена двумя срабатываниями на сессию), но корень —
отсутствие worktree для reviewer-сессий — остаётся входом в I02B.

Принятые без исправления пункты — в `REVIEW.md`, раздел «Принято без исправления».

## 6. Решение

Итерация прошла независимую верификацию двумя ролями с результатом **FAIL** (2 blocker,
11 major, 9 minor суммарно). Все blocker и major закрыты, каждый — с воспроизведением дефекта
исполнением до правки и, где применимо, с пробой мутацией на новый тест. Пять minor закрыты,
четыре приняты с обоснованием.

Оба blocker были не о коде домена, а о том, что **доказательства не доказывали заявленного**:
acceptance мог исполнять несобранный `dist` (то есть в CI не зеленел никогда), а разделение
ролей существовало только в миграции — приложение ходило под SUPERUSER. Это тот же класс, что
дал самые ценные Red в I01, и он снова пришёл не из реализации, а из проверки проверок.

**Рекомендация lead: короткий повторный проход только по изменённым файлам горячего пути**
(`command-handler.ts`, `principals.ts`, `world-db-cli.ts`, `security/policy.json`), затем
решение владельца. Основание — замечание архитектурного аудита: правки по M-1/M-2/M-5 меняют
схему и семантику ответа, а раунда, который проверит сами правки, план не предусматривал.

## 7. Что стоит знать перед I02B

- **Одна команда сейчас порождает ровно одно событие.** `decide` ставит каждому событию
  `sequence = state.sequence + 1`; команда с двумя событиями упала бы на уникальном индексе
  `(world_id, sequence)` — громко, но упала бы. Снять ограничение обязана I02B вместе со
  scheduled actions.
- **Позиции PRNG не персистятся.** `UnavailableRandomSource` в handler-е бросает исключение при
  любом розыгрыше — это намеренная ловушка: первая команда, которой понадобится случайность,
  упадёт с названной причиной, а не начнёт молча повторять одно значение.
- **Новая таблица не получает грантов автоматически** (проверено исполнением). Каждая таблица
  I02B обязана принести свои `grant` в той же миграции, иначе worker получит `permission denied`.
- **`event_id` больше не свободен.** Формат зафиксирован golden-тестами: изменение — migration.
  Ключ происхождения id — функция `eventIdOriginKey`, а не соглашение: scheduler обязан
  использовать её, иначе разойдётся молча.
- **Границы ядра — deny-list из шести имён.** `nanoid`, `uuid`, `luxon`, `axios` в
  `packages/domain` не поймает ни `boundaries:check`, ни `lint`. Инверсия в allow-list — вход
  в I02B.
- **Reviewer-сессии работают в общем дереве.** `pnpm task:worktree` покрывает исполнителей, но
  не верификацию; в I02A это дважды дало ложное нарушение владения.
- Роли PostgreSQL — объекты кластера, а не базы; их создаёт миграция 0003 идемпотентно, но при
  восстановлении в другой кластер их придётся создать до применения грантов.
