# I04 — Нужды, предметы, еда и отдых

**Статус:** в работе с 2026-08-29. Требование — SIM-03
([11_REQUIREMENTS_TRACEABILITY](../../11_REQUIREMENTS_TRACEABILITY.md)); conservation предметов
работает на SIM-07.

## 0. Условие входа: Gate A закрыт не полностью, и это записано, а не обойдено молча

Четыре пункта Gate A из пяти подтверждены измерением — досье в
[GATE-A-EVIDENCE](../I03-journey-to-browser/GATE-A-EVIDENCE.md). Пункт 3 («CI стабилен минимум на
10 последовательных запусках») на момент старта I04 не выполнен: до 2026-08-29 в `origin` был
только первый коммит, CI не запускался ни разу, а из среды разработки его результат не наблюдаем
— сеть закрыта.

Решение lead-а (владелец делегировал ход работы 2026-08-29): **I04 начинается, пункт 3
накапливается прогонами самой I04**. Основания:

- пункт 3 не закрывается работой над продуктом, он закрывается ВРЕМЕНЕМ и числом пушей. Ждать
  его, ничего не делая, — это не осторожность, а простой;
- каждый коммит I04 уходит в CI по тому же workflow, поэтому десять прогонов накопятся как
  побочный продукт итерации, а не вместо неё;
- локальное повторение гейта десять раз отвергнуто как замена: оно измеряет ЭТУ машину, а
  формулировка пункта говорит про CI. Подменять предмет измерения ради зелёной галочки — ровно
  тот класс дефекта, который итерации I02B и I03 ловили у себя чаще всего.

Цена названа: если CI окажется нестабильным, I04 останавливается и чинится CI. Это условие, а не
оговорка.

## 1. Гипотеза

Ресурсы и тело создают реальную необходимость действовать: агент, которого никто не кормит,
ухудшается наблюдаемо, а не молча меняет число внутри себя.

## 2. Observable demo

Один агент проходит цикл: голод растёт → агент ест → голод падает. Еда кончается — ухудшение
видно в ленте и на карточке агента, а не только в состоянии. На экране наблюдателя это читается
без знания устройства: «Грач голоден» появляется тогда же, когда событие в ленте.

Демонстрация обязана идти БЕЗ вмешательства оператора после старта: команда «поесть» приходит от
мира, а не от человека. До I05 (Utility AI) выбор действия задан простейшим правилом — «ест, если
голод перешёл в `critical` и еда есть»; это не интеллект, а прямое следствие порога.

## 3. Ключевое решение, которое надо принять ДО контрактов

**Как нужда меняется во времени, не порождая поток событий.**

Спецификация (`07_MVP_MECHANICS_SPEC` §5) говорит: «Нужды обновляются scheduled pulse-ами, но
событие публикуется только при пересечении значимого порога». Прямое прочтение противоречит
постоянному правилу проекта: canonical state меняется ТОЛЬКО событиями. Pulse, меняющий голод и
не публикующий события, ломает replay — состояние перестаёт быть функцией журнала.

Три варианта:

| | событий на сутки на агента | replay | спам |
| --- | --- | --- | --- |
| A. Pulse публикует событие всегда | ~96 при шаге 15 мин | цел | **да** — прямо назван STOP-условием |
| B. Pulse меняет состояние молча | 0 | **сломан** | нет |
| C. Нужда ВЫЧИСЛЯЕТСЯ, порог планируется | 1–3 | цел | нет |

**Принято C** (решение lead-а 2026-08-29, владелец делегировал). Каноническим состоянием становится не значение голода, а метка мирового
времени последнего приёма пищи. Голод — чистая функция от неё и текущего мирового времени, то
есть производная величина, а не факт. Момент пересечения следующего порога ВЫЧИСЛИМ заранее из
той же функции и коэффициентов ruleset, поэтому планируется тем же механизмом
`ScheduledAction`, что и прибытие: ровно одно событие на пересечение, ноль событий между ними.

Что это даёт:

- журнал остаётся тем, чем он есть, — списком фактов, а не выборкой измерений;
- `canonical_state` не меняется между событиями, поэтому checksum и replay не затронуты;
- «непросматриваемый поток событий» — STOP-условие итерации — становится невозможен
  конструктивно, а не благодаря удачно выбранному шагу pulse;
- I05 читает значение нужды в момент решения, и оно детерминировано.

Что это стоит:

- отклонение от буквы `07_MVP_MECHANICS_SPEC` §5. Спецификация ИСПРАВЛЕНА, а не обойдена:
  параграф про pulse-ы заменён разделом «Как нужда меняется во времени (решение I04)» с
  перечислением цены. Документ остаётся нормативным источником, а код — его следствием;
- функция нужды обязана быть монотонной и обратимой на участке между порогами, иначе «когда
  будет следующий порог» не вычислить. Для линейного роста это выполняется; при переходе к
  нелинейному потребуется либо кусочная линеаризация, либо возврат к A.

Вариант B отвергнут безусловно: он ломает SIM-01.

## 4. Scope

- нужды `hunger` и `fatigue` как производные от меток мирового времени в состоянии агента;
- пороги `normal < 0.45 <= warning < 0.75 <= critical` из versioned ruleset, а не из констант;
- события пересечения порога — в обе стороны (ухудшение И восстановление);
- инвентарь: владение предметом принадлежит ровно одному владельцу;
- команды `agent.eat` и `agent.rest`, их отказы с названной причиной;
- источники и стоки: еда исчезает при съедании, появляется только заявленным источником;
- отражение на экране наблюдателя: состояние нужды на карточке агента, события в ленте.

## 5. Out of scope

- выбор цели агентом (I05: Utility AI, hysteresis, планы);
- стресс, мораль, здоровье, раны, инфекция (I06/I07);
- торговля, цена, спрос (I08A–I08C);
- смерть от голода: правила тела — I07. Здесь голод доходит до `critical` и там остаётся.

## 6. Автоматическая проверка (из master plan)

- **conservation** — суммарное число предметов в мире меняется только событиями источника и
  стока; property-тест на последовательностях команд;
- **нельзя потратить дважды** — две команды `agent.eat` на один предмет: вторая отвергается,
  и отвергается по причине, а не по совпадению;
- **bounded needs** — значение нужды всегда в `[0, 1]`, включая границы и переполнение времени;
- **dead/inactive restrictions** — неактивный агент не ест;
- **batch-size independence** — размер пачки worker-а не меняет журнал (как в C4).

Плюс инварианты итерации:

- **ни одного события между порогами** — сутки мирового времени без вмешательства дают ровно
  столько событий, сколько было пересечений, и ни одним больше;
- **порог не пропускается** — скачок мирового времени через два порога подряд даёт два события
  в правильном порядке, а не одно последнее.

## 7. Ручной demo

Один агент, 24 игровых часа, 5 агентов в мире. Цикл голод → еда → восстановление наблюдается
в браузере. Отсутствие еды приводит к наблюдаемому ухудшению.

## 8. GO / STOP

**GO:** 24 игровых часа для 5 агентов без invalid state и без event spam.

**STOP/REWORK:** агент выживает без ресурсов; pulses создают непросматриваемый поток событий.

## 9. Препятствие, найденное чтением кода, и его настоящий размер

Решение C планирует пересечение порога обычным `ScheduledAction`. Первое чтение кода показало
препятствие, а второе — что оно вдвое меньше, чем я записал. Оставляю обе записи: разница между
ними и есть то, что отличает измерение от догадки.

**Что видно сразу.** `ScheduledAction.id` — это `event_id` события-причины
(`packages/domain/src/state.ts`), и на этом равенстве стоят снятие действия по `caused_by` в
`evolve`, вывод `command_id` из `action_id` и поле `caused_by_event_id` выводимой команды,
которое обязано быть валидным `evt:`-идентификатором. Нужды ломают равенство с двух сторон: одно
событие планирует два действия (у агента два порога), и самое первое планируется в генезисе, где
событий нет вовсе.

**Первый вывод — разделить `id` и `causedByEventId` — отвергнут.** Он верен по форме и слишком
дорог по существу: новое поле `ScheduledAction` меняет ФОРМУ канонического состояния, а `replay`
сверяет checksum состояния, полученного применением журнала к снимку. Снимки, записанные до I04,
после такой правки перестали бы сходиться, и цену пришлось бы платить миграцией снимков ради
удобства одной механики.

**Что показала проверка каждого из двух случаев по отдельности.**

| Кто планирует | Сколько действий | Есть ли событие-причина |
| --- | --- | --- |
| генезис мира | два на агента | **нет** |
| `agent.ate` | одно (следующий порог голода) | да |
| `need.threshold.crossed` | одно (следующий порог того же нужды) | да |
| `agent.rest` | одно (следующий порог усталости) | да |

Два действия из одного события — только в генезисе, а у генезиса события нет вовсе, поэтому у
его действий нет и причины, которую нужно было бы хранить. Все остальные случаи — ровно одно
действие на событие, то есть ровно та связь, которая уже есть.

**Настоящее препятствие меньше и лежит в другом месте:** `commandFor` в
`packages/persistence/src/scheduler.ts` БЕЗУСЛОВНО ставит `caused_by_event_id = action_id` и
`type: 'journey.complete'`. Это отображение обязано стать разбором по `kind` — не ради нужд, а
потому что действие другого вида порождает команду другого типа. Разбор по `kind` закрывает и
причинность: у `journey.complete` она есть и остаётся прежней, у `need.threshold` её нет, и поле
просто отсутствует, а не подделывается синтетическим `evt:`-идентификатором.

Поэтому форма канонического состояния для существующих действий не меняется вовсе, снимки и
журналы до I04 применяются как записаны, а новый вид действия получает свободный
детерминированный ключ `sched:need:<agent>:<need>:<dueAt>`.

**Чем ключ не является.** Он не «совпадение полей», от которого предостерегал M7: у одного
агента по одной нужде не может быть двух ждущих пересечений одновременно — это инвариант
конструкции, и он проверяется тестом, а не подразумевается.

## 10. Порядок работ

Итерация нарезана на срезы, каждый со своим Red → Green и своим коммитом. Acceptance на
observable demo §2 пишется красным ПОСЛЕ contract freeze и остаётся красным до среза C.

- **A. Голод и усталость как производные величины.** Ruleset, чистая функция нужды, команда
  пересечения, событие `need.threshold.crossed`, планирование следующего порога, разбор
  `commandFor` по `kind`, миграция расписания под второй вид действия.
- **B. Предметы, `agent.eat` и `agent.rest`.** Инвентарь, conservation, отказы с названной
  причиной, восстановление нужды.
- **C. Проекция и экран.** Состояние нужды на карточке агента, события в ленте.
- **D. Независимый review двумя ролями, затем полный gate.**

## 11. Изменения замороженных ожиданий

Здесь перечислены тесты, чьи ожидания изменены этой итерацией, и объяснение изменившегося
поведения. Список ведётся по ходу работы; пустая строка означала бы, что итерация ничего не
меняла в контрактах.

| Тест | Что изменилось | Почему это изменение поведения, а не подгонка |
| --- | --- | --- |
| `command.contract.test.ts` — «перечисляет ровно объявленные типы команд» | добавлен `need.threshold.cross` | Пин каталога намеренно точен («не `хотя бы содержит`») и требует осознанной правки: он ловит тип, попавший в контракт без решения. Решение принято и записано — §3 и §5 плана |
| `world-event.contract.test.ts` — «перечисляет типы событий первого slice» | добавлен `need.threshold.crossed` | То же основание. §12 контрактов запрещает заводить схемы БУДУЩИХ срезов заранее; I04 и есть срез, вводящий эту механику |
| `i03-d1-d2-tempo-does-not-change-the-world` — D1 и D2 | журналы сравниваются ПРЕФИКСОМ по мировому времени, а не целиком; момент выдачи команд закреплён явным `--at-world-time` | Изменилось поведение мира, а не ожидание. До нужд события порождались только командами, поэтому быстрый мир за то же реальное время проживал ровно те же восемь фактов. Теперь само течение времени рождает события: мир при темпе 600 успевает прожить двое суток там, где мир при темпе 6 — два часа, и лишние события у него это прожитая жизнь, а не расхождение. Утверждение «темп не меняет мир» проверяемо только за одинаковое МИРОВОЕ время. Момент выдачи команды стал настоящим входом (см. §12) и потому закреплён явно |
| `ruleset.test.ts` — «стабильные тестовые версии» | `rulesVersion` 0.1.0 → 0.2.0 | Rules bundle хешируется от СОДЕРЖИМОГО ruleset; появление коэффициентов при прежней версии дало бы два разных мира под одним именем правил |
| `registry.test.ts` — золотой реестр миграций | добавлены 0014 и 0015 | Пин намеренно ловит незамеченную миграцию; обе добавлены осознанно и описаны в своих файлах |
| `upgrade-path.integration.test.ts` — писатель поставки N-1 | список колонок стал параметром рубежа | 0014 добавила первую ОБЯЗАТЕЛЬНУЮ колонку. Пока все добавленные колонки были nullable, один список молча обслуживал два разных рубежа; тест упал на вставке и назвал это сам |

## 12. Дефект, найденный исполнением: горизонт снова убежал от мирового времени

Починка кредита темпа из I03 держалась на условии «мир бывает пустым»: пока миру нечего делать,
worker переставляет точку отсчёта, и разрыв между горизонтом и мировым временем не копится.

**Нужды это условие отменили.** У мира ВСЕГДА есть ждущее действие — следующий порог голода, —
поэтому отсчёт не переставляется никогда, и горизонт уходит вперёд всё время простоя. Сам разрыв
безвреден и необходим: без него действие, назначенное на будущее, не наступило бы вовсе
(замкнутый круг ADR-004).

Ломалось другое: внешняя команда штамповалась ТЕКУЩИМ мировым временем, то есть моментом
последнего события, уже далеко позади горизонта. Путь, начатый «сейчас», получал время прибытия
в прошлом и завершался на первом же шаге. Измерено: сорокаминутный путь занимал 0.7 реальной
секунды вместо десяти — тот же наблюдаемый дефект, что в I03, по другой причине.

**Починка.** «Который час в мире» перестал быть внутренним состоянием worker-а: горизонт
записывается в `worlds.observed_world_time` (миграция 0015, операционная колонка — не в
`WorldState`, не в checksum, не в replay). Внешняя команда штампуется им, а не временем
последнего события, и получает время прибытия в будущем.

**Следствие, которое пришлось признать честно:** момент выдачи команды стал настоящим ВХОДОМ
мира. У миров с разной скоростью он разный, поэтому утверждение D2 «темп не меняет журнал» верно
только при закреплённом моменте выдачи. Флаг `--at-world-time` делает вход явным, а сравнение
журналов ведётся префиксом по мировому времени.

## 13. Отклонения от этого плана

Заполняется по ходу итерации.
