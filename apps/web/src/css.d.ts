/**
 * Побочный импорт CSS — часть модели Next.js: стиль подключается к странице, а не к модулю
 * JavaScript. TypeScript о таком импорте не знает, и без этого объявления сборка приложения
 * расходилась бы с проверкой типов.
 */
declare module '*.css';
