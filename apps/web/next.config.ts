import type { NextConfig } from 'next';

/**
 * Экран наблюдателя (I03).
 *
 * `output: 'standalone'` не нужен: продукт пока запускается локально, а лишняя сборочная
 * конфигурация — это то, что потом никто не решается тронуть. Появится вместе с I17.
 */
const config: NextConfig = {
  reactStrictMode: true,
};

/**
 * Адреса observer API здесь НЕТ намеренно. Первая редакция клала его в `env`, и это оказалось
 * ошибкой: Next подставляет такие значения в браузерный бандл НА СБОРКЕ, поэтому один и тот же
 * собранный экран нельзя было направить на другой API. Обнаружилось живым прогоном E2E — сценарий
 * поднимал API на своём порту, а страница продолжала стучаться на порт, зашитый при сборке.
 *
 * Адрес читается на сервере при запросе и приходит в компонент свойством (`page.tsx`).
 */

export default config;
