{
  "$schema": "https://turbo.build/schema.json",
  "ui": "stream",
  "globalDependencies": ["tsconfig.base.json", ".nvmrc"],
  "//": "Next.js кладёт сборку в `.next`, а не в `dist`, поэтому у @zona/web свой список выходов (`@zona/web#build`). Без него turbo кэшировал для web ПУСТОЙ набор: при тёплом кэше `pnpm build` отвечал «14 successful … FULL TURBO», не создавая `apps/web/.next` вовсе, и следом падал `next start` — демо и E2E ломались именно на машине, где сборка «уже прошла». Найдено предупреждением самого turbo при сборке docker-образа: «no output files found for task @zona/web#build». `.next/cache` исключён намеренно: это локальный кэш инструмента, а не артефакт сборки. Комментарий стоит на верхнем уровне, а не внутри `tasks`: ключ `//` среди задач turbo принимает за имя задачи и падает разбором.",
  "tasks": {
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "@zona/web#build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"]
    }
  }
}
