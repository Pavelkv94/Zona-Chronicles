/**
 * E2E-сценарий journey до браузера (I03, D13).
 *
 * ## Почему сценарий поднимает мир САМ, а не ждёт запущенного
 *
 * D13 проверяет цепочку целиком: команда CLI → канонический журнал → outbox → проекция → API →
 * экран. Прогон против чужого, уже работающего мира доказывал бы только «страница что-то
 * показывает»: неизвестно, ЧЬИ это события и не остались ли они от прошлого раза. Поэтому
 * сценарий создаёт свою базу, свой мир и свои процессы — и удаляет их за собой.
 *
 * ## Один worker, без параллелизма
 *
 * `workers: 1` и `fullyParallel: false` не оптимизация наоборот, а необходимость: сценарии делят
 * один мир и один набор портов, а мир по построению один на процесс. Параллельный прогон дал бы
 * ровно тот класс ложных падений, который уже стоил раунда верификации в I02B — когда падает
 * не тест, а его сосед.
 */
import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: 'tests/e2e',
  // Поднятие стека (миграции, мир, три процесса) занимает заметно больше минуты; тайм-аут
  // описывает реальную стоимость, а не желаемую.
  timeout: 180_000,
  expect: { timeout: 15_000 },
  fullyParallel: false,
  workers: 1,
  forbidOnly: true,
  retries: 0,
  reporter: [['list']],
  use: {
    baseURL: 'http://localhost:3100',
    trace: 'retain-on-failure',
  },
});
