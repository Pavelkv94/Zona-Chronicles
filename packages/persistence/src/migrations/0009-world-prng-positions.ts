/**
 * 0009 — позиции потоков PRNG становятся частью мира (M4 аудита I02B).
 *
 * До этой миграции позиции жили ТОЛЬКО в снимках. Снимок — точка восстановления, которую
 * оператор берёт по своему решению (OPS-04), а не непрерывная запись: между двумя снимками мир
 * принимает сколько угодно команд. Источник случайности, стартующий с позиций последнего снимка,
 * повторил бы уже сделанные розыгрыши, то есть выдал бы одни и те же значения дважды при том же
 * seed — прямое нарушение SIM-01, которое ничем бы не проявилось, кроме подозрительно похожего
 * мира.
 *
 * Позиция обязана двигаться там же, где двигается `last_sequence`: в транзакции команды. Поэтому
 * колонка живёт в `worlds`, а не в отдельной таблице — тот же UPDATE, та же атомарность, тот же
 * замок. Снимок её фотографирует, а не хранит единственный экземпляр.
 *
 * `default '{}'` для уже существующих миров корректен по построению: до этой миграции ни одна
 * команда не могла сделать розыгрыш (`UnavailableRandomSource` бросал исключение), поэтому у
 * любого существующего мира позиции пусты по факту, а не по умолчанию.
 *
 * Один элемент `statements` — ровно один SQL-оператор без завершающей точки с запятой.
 */
import type { Migration } from './types.ts';

const statements: readonly string[] = [
  `alter table worlds add column prng_stream_positions jsonb not null default '{}'::jsonb`,
];

/** `phase: 'expand'` — добавляется колонка со значением по умолчанию; данные не переносятся. */
export const worldPrngPositionsMigration: Migration = {
  id: '0009',
  name: 'world-prng-positions',
  phase: 'expand',
  statements,
};
